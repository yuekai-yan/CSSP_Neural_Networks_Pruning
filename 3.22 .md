## 3.22
### 需要手动调试选择合适参数 -> keep_ratio / tolerence
```python
pruned_model_iter = iterative_pruning(model2, X, (1,28,28), rho, step_size, "StrongRRQR")
### rho is fraction of flops remaining, step_size is keep ratio for each layer
```
- keep_ratio过大时，只有ARP比较稳定
- StrongRRQR 中 keep_ratio过大会导致 R(k,k)过小，从而无法解triangular

```python
--> 126 AB = solve_triangular(R[:k, :k], R[:k, k:])
    128 # gamma[j] = || R22[:, j] ||_2,  shape (n-k,)
    129 gamma = np.linalg.norm(R[k:, k:], axis=0) if k < Rm else np.zeros(n - k)

LinAlgError: singular matrix: resolution failed at diagonal 55
```

- RPCholseky 中 keep_ratio过大会导致d过小，无法构建概率分布完成`np.random.choice`
```python
File ~/Documents/CSSP_Neural_Networks_Pruning/CSSP/RPCholesky.py:23, in RPCholesky(A, k)
     21 prob = []
     22 prob = d / d.sum()
---> 23 jk = np.random.choice(len(prob), p=prob)
...
     25 g = M[:, jk]

File numpy/random/mtrand.pyx:995, in numpy.random.mtrand.RandomState.choice()

ValueError: probabilities contain NaN
```
- 如果设置为tolerence, 仍需要手动调试选择合适的tolerence