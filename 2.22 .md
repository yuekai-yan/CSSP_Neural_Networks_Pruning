## Adaptive Randomized Pivoting
- How to compute V ?
- How to get T from Adaptive Randomized Pivoting ?
$$
\begin{aligned}
\Pi_J 
&= A(:, J) 
[A(:, J)^{T} A(:, J)]^{\dagger} 
A(:, J)^{T} \\
&= A(:, J) A(:, J)^{\dagger}
\end{aligned}
$$

$$
    T = A(:, J)^{\dagger} A
$$

- Equation (2.6)
- How to compute $u_k$ in (2.7) / step 6 in Algorithm 2.2 ? (backslash) ？

## Implementation
- Input :    \.       Output: